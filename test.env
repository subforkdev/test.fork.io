#!/usr/bin/env envstack
include: [default]
all: &all
  ENVPATH: ${PWD}:${ENVPATH}
  PATH: ${PWD}/bin:${PWD}/build/bin:${PATH}
  PYTHONPATH: ${PWD}/lib:${PWD}/build
darwin:
  <<: *all
linux:
  <<: *all
windows:
  <<: *all
